export default class ApiPokemon {
  constructor(id, name, height, weight, base_experience) {
      this.id = id;
      this.name = name;
      this.height = height;
      this.weight = weight;
      this.base_experience = base_experience;
  }
}
